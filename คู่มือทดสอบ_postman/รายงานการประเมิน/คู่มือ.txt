ผมจัดแพ็กชุด SQL + Worksheet ให้พร้อมใช้งานแล้ว:

ไฟล์ SQL

1_topic_rates.sql — สรุปอัตราสำเร็จรายหัวข้อ (0..1) + ส่วนคะแนนบน 60

2_normalized60.sql — คำนวณคะแนนรวม Normalized/60 ต่อคน/ต่อรอบ

3_topic_breakdown_single_evaluatee.sql — แยกรายหัวข้อสำหรับผู้ถูกประเมินคนเดียว

Worksheet (สำหรับใช้สอน/ทำใบงาน)

Worksheet_Normalized60_A5.docx

แนวทางใช้งานย่อ:

เปิดไฟล์ 2_normalized60.sql แล้วแทนค่า :period_id เป็นรอบที่ต้องการ (เช่น 1) เพื่อดูคะแนนรวม 60

ใช้ 1_topic_rates.sql เพื่อดู topic_rate_0_1 และส่วนคะแนนของแต่ละหัวข้อ

ใช้ 3_topic_breakdown_single_evaluatee.sql ใส่ :period_id และ :evaluatee_id เพื่อตรวจรายละเอียดของรายบุคคล

ถ้าต้องการเวอร์ชัน MySQL 5.7-only (ไม่มี CTE), เวอร์ชัน Knex.js raw หรือให้แปลงเป็น PDF สำหรับแจกนักศึกษา บอกผมได้ครับ เดี๋ยวจัดให้ทันที 🙌